/**
 * Intrinsic dimensions for repeated brand / UI assets (CLS + Lighthouse).
 * Values match files under `public/images/` (verified via sharp metadata).
 */
export const LOGO_INTRINSIC = { width: 818, height: 235 } as const;

/** Hero illustration `public/images/home/banner.webp` */
export const HERO_BANNER = { width: 494, height: 494 } as const;

/** Decorative star `public/images/star.webp` */
export const STAR_ICON = { width: 64, height: 64 } as const;
